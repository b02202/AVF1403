// Robert Brooks
// AVF 1403
// Project 1

var container = Ti.UI.createView ({
	height: '100%',
	width: '100%',
	backgroundColor: '#000',
	backgroundImage: 'bg.png'
});

var logo = Ti.UI.createImageView({
	image: 'logo.png',
	bottom: '60%'
});
var headline = Ti.UI.createLabel({
	text: "Client Accountability App",
	font: {fontSize: 25, fontFamily: "Arial"},
	color: '#fff',
	top: '41%'
});
var nutButton = Ti.UI.createButton({
	title: 'NUTRITION',
	font: {fontSize: 25, fontFamily: "Arial"},
	color: '#fff',
	backgroundColor: '#0099ff',
	borderColor: '#fff',
	width: '50%',
	height: 75,
	bottom: 0,
	left: 0
});

var exButton = Ti.UI.createButton({
	title: 'EXERCISE',
	font: {fontSize: 25, fontFamily: "Arial"},
	color: '#fff',
	backgroundColor: '#0099ff',
	borderColor: '#fff',
	width: '50%',
	height: 75,
	bottom: 0,
	right: 0
});


// Form Layout
var formView = Ti.UI.createScrollView({
	layout: "vertical",
	//backgroundColor: "#000",
	//borderRadius: 20,
	//borderColor: "#fff",
	top: headline.top + 30,
	left: '30%',
	right: '30%',
	bottom: '50'
});

// Form Fields
var fieldHeight = 40;
var emailTxt = Ti.UI.createTextField({
	  borderStyle: Ti.UI.INPUT_BORDERSTYLE_ROUNDED,
	  color: '#336699',
	  top: 10,
	  left: 10,
	  right: 10, 
	  height: fieldHeight,
	  hintText: "email@email.com"
});
var passTxt = Ti.UI.createTextField({
	  borderStyle: Ti.UI.INPUT_BORDERSTYLE_ROUNDED,
	  color: '#336699',
	  top: 10,
	  left: 10,
	  right: 10, 
	  height: fieldHeight,
	  hintText: "Password"
});

container.add(logo, headline, nutButton, exButton);
win1.add(container);
